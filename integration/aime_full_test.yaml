# Schema 1 MCTS Optimization Configuration
# 纯粹的MCTS树搜索优化（不涉及RL融合）
device: "cuda"

# 训练配置
total_epochs: 5            # 总epoch数
episodes_per_epoch: 3      # 每个epoch的episode数（每个episode运行一轮MCTS）
save_frequency: 1          # 每个epoch保存一次checkpoint

# 输出目录（Google Drive）
output_dir: "/content/drive/MyDrive/agentflow/outputs/aime_full_test"

# 环境配置
environment:
  # 数据集配置
  dataset: "AIME"

  # MCTS配置
  max_rounds: 5              # MCTS树搜索最大轮数
  sample: 24                 # 每轮评估的样本数

  # 数据集划分
  train_test_split: 0.8      # 80% 训练集，20% 测试集
  mini_batch_size: 6         # Mini-batch大小（随机采样6题测试）

  # Operators列表
  operators:
    - "Custom"               # 基础operator
    - "ScEnsemble"           # 自一致性集成
    - "Review"               # 审查
    - "Revise"               # 修订

  # LLM配置
  opt_llm_config:
    model: "gpt-4o-mini"
    key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"
    temperature: 0.9

  exec_llm_config:
    model: "gpt-4o-mini"
    key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"
    temperature: 0.7

logging:
  level: "INFO"
