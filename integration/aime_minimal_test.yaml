# Schema 1 MCTS Optimization - Minimal Configuration
# 用于快速测试的最小化配置
device: "cuda"

# 最小化训练配置（快速验证）
total_epochs: 1            # 仅1个epoch
episodes_per_epoch: 1      # 仅1个episode
save_frequency: 1

# 输出目录（Google Drive）
output_dir: "/content/drive/MyDrive/agentflow/outputs/aime_minimal_test"

# 环境配置
environment:
  # 数据集
  dataset: "AIME"

  # MCTS配置（最小化）
  max_rounds: 2              # 最少2轮
  sample: 6                  # 极小采样集

  # 数据集划分
  train_test_split: 0.8
  mini_batch_size: 2         # Mini-batch: 2题/测试

  # Operators（最小化）
  operators:
    - "Custom"
    - "ScEnsemble"

  # LLM配置
  opt_llm_config:
    model: "gpt-4o-mini"
    key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"
    temperature: 0.9

  exec_llm_config:
    model: "gpt-4o-mini"
    key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"
    temperature: 0.7

logging:
  level: "INFO"
